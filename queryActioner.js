class queryActioner {
    query

    constructor(query) {
        this.query = query;
    }

    getQuery(){
        return this.query;
    }

    setQuery(query) {
        this.query = query;
    }
}

